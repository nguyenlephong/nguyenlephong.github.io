export const NOTIFICATION_MESSAGE = {
  position: "bottom",
  duration: 4
}

export const LANGUAGE_DEFAULT = process.env.REACT_APP_LANGUAGE_DEFAULT

const globalConfig = {
  NOTIFICATION_MESSAGE,
  LANGUAGE_DEFAULT
}

export default globalConfig;